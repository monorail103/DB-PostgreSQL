講義資料（第09回～第12回）に含まれる「SQLドリル」の問題をすべて抽出・整理しました。
試験対策として「そらで書けるようにしておく」必要はありませんが、A4メモに記述する内容の選定や、実際の演習に活用してください。

---

### 第09回：テーブル定義 (DDL)

| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-01_1.sql** | `p_characters` テーブルを作成する `CREATE TABLE` 文を記述せよ。<br>・`character_id`: 主キー、自動採番(1000開始)、手動設定可<br>・`public_id`: UUID、非NULL、一意、デフォルト自動生成<br>・`name`: 3-16文字英字のみ、非NULL、一意<br>・`hair_color`: 7文字固定('#'で始まるカラーコード)、非NULL<br>・`hp_max`: 0以上の整数、非NULL<br>・`hp`: 0以上かつ`hp_max`以下、非NULL<br>・`blood_type`: 'A','B','O','AB'のみ、NULL許可<br>・`height_cm`: 50.0-250.0、非NULL<br>・`created_on`: 2025/12/1～現在、非NULL<br>・`deleted_on`: `created_on`以降、NULL許可 |
| **ex-02_1.sql** | 指定された論理ER図（users, articles, comments, categories）と仕様に基づくテーブル群を定義するSQLを記述せよ。<br>・外部キー制約、デフォルト値、CHECK制約などを適切に設定すること。 |

---

### 第10回：データ抽出・集計・サブクエリ・内部結合

#### 基本的な抽出と集計
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-01_1.sql** | `x_items` から全レコードを指定フォーマットで出力（`item_id`昇順）。 |
| **ex-01_2.sql** | `x_jobs` から全レコードを指定フォーマットで出力（`job_id`昇順）。 |
| **ex-01_3.sql** | `x_character_items` から全レコードを出力（`item_id`, `character_id`昇順）。 |
| **ex-01_4.sql** | `x_character_items` から、各アイテムの「所持キャラ数」「所持総数」を集計。 |
| **ex-01_5.sql** | `x_character_items` から、所持総数が6個以上のアイテムを集計（`HAVING`句）。 |
| **ex-01_6.sql** | `x_jobs` から `name` が 'Priest' の `job_id` を出力。 |

#### サブクエリ (結合なし)
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-02_1.sql** | どのキャラも所持していないアイテムを抽出。 |
| **ex-02_2.sql** | 全キャラ通算の所持合計が5個以下のアイテムを抽出。 |
| **ex-02_3.sql** | どのギルドにも所属していないキャラを抽出。 |
| **ex-02_4.sql** | ジョブが Wizard か Priest のキャラ（論理削除済みを除く）を抽出。 |
| **ex-02_5.sql** | 所持しているキャラが2名以下のアイテムを抽出（`COUNT`と`HAVING`）。 |
| **ex-02_6.sql** | 重量が最大のアイテムを抽出。 |
| **ex-02_7.sql** | 各キャラが所持するアイテムの種類数を集計（相関サブクエリ）。 |

#### 内部結合 (INNER JOIN)
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-03_1.sql** | キャラとギルドを結合し、ギルド名、オーナー名、オーナーレベルを出力。 |
| **ex-03_2.sql** | ex-03_1 と同じ結果をサブクエリで実現せよ。 |
| **ex-03_3.sql** | キャラと所持アイテムを結合し、一覧を出力。 |
| **ex-03_4.sql** | Wizard か Priest のキャラ（論理削除除く）を内部結合で抽出（ex-02_4と比較）。 |
| **ex-03_5.sql** | 通算所持数が6個以上のアイテムを内部結合で抽出（ex-01_5と比較）。 |
| **ex-03_6.sql** | ジョブごとのキャラ人数を集計（論理削除除く）。0人のジョブは出なくてよい。 |
| **ex-03_7.sql** | ex-03_6 に全ジョブ合計（Total）を追加（`ROLLUP`）。 |

#### 3テーブル以上の結合
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-04_1.sql** | ギルドごとのメンバー一覧を出力（オーナーかどうかのフラグ、レベル降順など）。 |
| **ex-04_2.sql** | ギルドごとの統計（オーナー名、メンバー数、平均レベル）を出力。 |
| **ex-04_3.sql** | ギルドごとに、ジョブ別の人数を集計。 |

---

### 第11回：外部結合 (OUTER JOIN)

#### 復習と応用 (サブクエリ・EX)
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-01_1.sql** | サブクエリを使い、キャラ名とジョブ名を出力（Zachなどジョブなしも含む）。 |
| **ex-01_2.sql** | サブクエリを使い、ジョブ別人数を集計（0人のジョブも出す）。 |
| **ex-01_3.sql** | ジョブがNULLの場合に '---' と表示（`COALESCE`）。 |
| **ex-01_4.sql** | (EX) 各ジョブの最高レベルのキャラを出力。 |
| **ex-01_5.sql** | (EX) アイテムセット販売（2種類の合計の3割引）で3500G以上になる組み合わせを出力。 |

#### 外部結合 (LEFT/RIGHT JOIN)
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-02_1.sql** | `n_characters` と `n_jobs` から一覧出力（右外部結合を使用）。ジョブなしは '---'。 |
| **ex-03_1.sql** | アイテム基準で所持キャラ一覧を出力（外部結合）。誰も持っていないアイテムも出す。 |
| **ex-03_2.sql** | アイテム基準で所持キャラとジョブを出力。NULL処理に注意。 |
| **ex-03_3.sql** | キャラ基準で所持アイテム総数を出力。アイテム未所持のキャラ（0個）も出す。 |
| **ex-03_4.sql** | アイテム基準で所持人数を集計。 |

#### 完全外部結合 (FULL JOIN)
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-04_1.sql** | 指定された形式（アイテム名と特定のキャラの所持数をマトリクス風に比較）の結果を完全外部結合で記述せよ。 |

---

### 第12回：応用操作 (INSERT/UPDATE, etc)

#### 外部結合とWHEREの罠
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-01_1** | 論理削除されたキャラ**だけ**をジョブ基準で列挙・集計したいが、意図通り動かないSQLを修正せよ（`WHERE`ではなく`ON`に条件を書く等）。 |
| **ex-01_2** | 全アイテムについて所持キャラを列挙（論理削除済みキャラは除外）。 |
| **ex-01_3** | 全アイテムについて総所持数と所持人数を集計（論理削除済みキャラは除外）。 |

#### サブクエリを用いたINSERT
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-02_1** | `x_gold_transfers` のSELECT結果を指定フォーマット（UUID短縮、NULL置換など）に整形。 |
| **ex-03_1** | お年玉：全キャラ（論理削除除く）に25,000～45,000Gのランダム額をシステムから送金。 |
| **ex-03_2** | 共益費：各ギルドメンバーからオーナーへ2,000Gを送金（オーナー自身は除く）。 |
| **ex-03_3** | 初期資金：各キャラ作成日時の5分後に1,000Gをシステムから送金。 |
| **ex-03_4** | 応援CP：Priest/Wizardのキャラ（論理削除除く）に `Level * 1000G` を送金。 |

#### サブクエリを用いたUPDATE
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-04_1** | Priest/Wizard以外のキャラのレベルを+1する（論理削除除く）。 |
| **ex-04_2** | 'High Mana Potion'所持者のレベルを+2する。 |
| **ex-04_3** | 流通数（全所持数）が6個以上のアイテム価格を1.2倍（10円単位切り上げ）にする。 |
| **ex-04_4** | アイテム価格を「所持人数 × 50G」値上げ（所持者0なら100G値下げ）する。 |
| **ex-04_5** | 各キャラのレベルを「そのジョブに就いている人数」分だけ上げる。 |

#### FROM句を用いたUPDATE (EX課題)
| ファイル名 | 問題概要 |
| :--- | :--- |
| **ex-05_1** | ex-04_1 の内容を `FROM` 句を用いたUPDATE文で記述せよ。 |
| **ex-05_2** | ex-04_2 の内容を `FROM` 句を用いたUPDATE文で記述せよ。 |
| **ex-05_3** | ex-04_3 の内容を `FROM` 句を用いたUPDATE文で記述せよ。 |
| **ex-05_4** | ex-04_4 の内容を `FROM` 句を用いたUPDATE文で記述せよ。 |
| **ex-05_5** | ex-04_5 の内容を `FROM` 句を用いたUPDATE文で記述せよ。 |